{
  "name": "typescript-graphql",
  "scripts": {
    "start": "node dist/graphql",
    "clean": "rm -rf dist",
    "build": "npm -s run generate",
    "build-local": "npm -s run clean && npm -s run generate && tsc",
    "postinstallo": "echo WOOT_POST_INSTALL && npm -s run generate",
    "generate": "npm -s run generate:prisma && npm -s run generate:nexus",
    "generate:prisma": "prisma generate",
    "generate:nexus": "GENERATE_ARTIFACTS=true ts-node --transpile-only api/_lib/schema",
    "dev": "ts-node-dev --no-notify --respawn --transpileOnly api/graphql"
  },
  "dependencies": {
    "@prisma/client": "2.0.0-beta.1",
    "@types/node-fetch": "2.5.5",
    "apollo-server-lambda": "2.12.0",
    "graphql": "14.6.0",
    "@nexus/schema": "0.13.1",
    "nexus-prisma": "0.12.0"
  },
  "devDependencies": {
    "@prisma/cli": "2.0.0-beta.1",
    "@types/node": "12.12.34",
    "ts-node": "8.8.2",
    "ts-node-dev": "1.0.0-pre.44",
    "typescript": "3.8.3"
  },
  "prettier": {
    "singleQuote": true,
    "semi": false,
    "trailingComma": "all"
  },
  "engines": {
    "node": "12.x"
  }
}
